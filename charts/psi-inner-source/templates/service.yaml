apiVersion: v1
kind: Service
metadata:
  name: {{ include "psi-inner-source.fullname" . }}
  labels:
    {{- include "psi-inner-source.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "psi-inner-source.selectorLabels" . | nindent 4 }}
